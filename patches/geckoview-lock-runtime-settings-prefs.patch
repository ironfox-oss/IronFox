diff --git a/mobile/android/geckoview/src/main/java/org/mozilla/geckoview/RuntimeSettings.java b/mobile/android/geckoview/src/main/java/org/mozilla/geckoview/RuntimeSettings.java
index dcd5d39ad9a3b..b1791288f1759 100644
--- a/mobile/android/geckoview/src/main/java/org/mozilla/geckoview/RuntimeSettings.java
+++ b/mobile/android/geckoview/src/main/java/org/mozilla/geckoview/RuntimeSettings.java
@@ -106,7 +106,7 @@ public abstract class RuntimeSettings implements Parcelable {
      */
     public void commit(final T newValue) {
       if (newValue.equals(mValue)) {
-        return;
+//        return;
       }
       set(newValue);
       commit();
diff --git a/mobile/shared/components/geckoview/GeckoViewPreferences.sys.mjs b/mobile/shared/components/geckoview/GeckoViewPreferences.sys.mjs
index a795b36e6a8f8..5c71ce7746408 100644
--- a/mobile/shared/components/geckoview/GeckoViewPreferences.sys.mjs
+++ b/mobile/shared/components/geckoview/GeckoViewPreferences.sys.mjs
@@ -7,6 +7,7 @@ import { GeckoViewUtils } from "resource://gre/modules/GeckoViewUtils.sys.mjs";
 const lazy = {};
 ChromeUtils.defineESModuleGetters(lazy, {
   EventDispatcher: "resource://gre/modules/Messaging.sys.mjs",
+  RFPHelper: "resource://gre/modules/RFPHelper.sys.mjs",
 });
 
 const { PREF_STRING, PREF_BOOL, PREF_INT } = Ci.nsIPrefBranch;
@@ -129,17 +130,47 @@ export const GeckoViewPreferences = {
         ? Services.prefs
         : Services.prefs.getDefaultBranch(null);
     try {
+      if (setBranch == "user" && Services.prefs.prefIsLocked(pref) === true) {
+        Services.prefs.unlockPref(pref);
+      };
       switch (type) {
         case PREF_STRING:
-          branch.setStringPref(pref, value);
+          if (branch.getStringPref(pref) !== value) {
+            if (setBranch !== "user" && Services.prefs.prefIsLocked(pref) === true) {
+                Services.prefs.unlockPref(pref);
+            }
+            branch.setStringPref(pref, value);
+          };
+          if (Services.prefs.prefIsLocked(pref) !== true) {
+            Services.prefs.lockPref(pref);
+          };
           return true;
 
         case PREF_BOOL:
-          branch.setBoolPref(pref, value);
+          if (branch.getBoolPref(pref) !== value) {
+            if (setBranch !== "user" && Services.prefs.prefIsLocked(pref) === true) {
+                Services.prefs.unlockPref(pref);
+            };
+            branch.setBoolPref(pref, value);
+          };
+          if (Services.prefs.prefIsLocked(pref) !== true) {
+            Services.prefs.lockPref(pref);
+          };
           return true;
 
         case PREF_INT:
-          branch.setIntPref(pref, value);
+          if (branch.getIntPref(pref) !== value) {
+            if (setBranch !== "user" && Services.prefs.prefIsLocked(pref) === true) {
+                Services.prefs.unlockPref(pref);
+            };
+            branch.setIntPref(pref, value);
+            if (pref === "privacy.spoof_english") {
+                lazy.RFPHelper._handleSpoofEnglishChanged();
+            };
+          };
+          if (Services.prefs.prefIsLocked(pref) !== true) {
+            Services.prefs.lockPref(pref);
+          };
           return true;
 
         default:
diff --git a/mobile/shared/components/geckoview/GeckoViewStartup.sys.mjs b/mobile/shared/components/geckoview/GeckoViewStartup.sys.mjs
index 80214c8f38e91..75b2ed2de574d 100644
--- a/mobile/shared/components/geckoview/GeckoViewStartup.sys.mjs
+++ b/mobile/shared/components/geckoview/GeckoViewStartup.sys.mjs
@@ -340,23 +340,107 @@ export class GeckoViewStartup {
       }
       case "GeckoView:ResetUserPrefs": {
         for (const name of aData.names) {
-          Services.prefs.clearUserPref(name);
+//          Services.prefs.clearUserPref(name);
         }
         break;
       }
       case "GeckoView:SetDefaultPrefs": {
         const prefs = Services.prefs.getDefaultBranch("");
+        const ifPrefix = "browser.ironfox.geckoRuntimeSettings.";
+        const ifSuffix = ".value";
+        const ifModified = ".modified";
         for (const [name, value] of Object.entries(aData)) {
           try {
             switch (typeof value) {
               case "string":
-                prefs.setStringPref(name, value);
+                if (prefs.getStringPref(name) !== value) {
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}`);
+                    };
+                    Services.prefs.setStringPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    prefs.setStringPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    if (Services.prefs.prefIsLocked(name) === true) {
+                        Services.prefs.unlockPref(name);
+                    };
+                    Services.prefs.setStringPref(name, value);
+                    prefs.setStringPref(name, value);
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                    };
+                    if (Services.prefs.getBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                        Services.prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                        prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                    };
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}`);
+                };
+                if (Services.prefs.prefIsLocked(name) !== true) {
+                    Services.prefs.lockPref(name);
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                };
                 break;
               case "number":
-                prefs.setIntPref(name, value);
+                if (prefs.getIntPref(name) !== value) {
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}`);
+                    };
+                    Services.prefs.setIntPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    prefs.setIntPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    if (Services.prefs.prefIsLocked(name) === true) {
+                        Services.prefs.unlockPref(name);
+                    };
+                    Services.prefs.setIntPref(name, value);
+                    prefs.setIntPref(name, value);
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                    };
+                    if (Services.prefs.getBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                        Services.prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                        prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                    };
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}`);
+                };
+                if (Services.prefs.prefIsLocked(name) !== true) {
+                    Services.prefs.lockPref(name);
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                };
                 break;
               case "boolean":
-                prefs.setBoolPref(name, value);
+                if (prefs.getBoolPref(name) !== value) {
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}`);
+                    };
+                    Services.prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}`, value);
+                    if (Services.prefs.prefIsLocked(name) === true) {
+                        Services.prefs.unlockPref(name);
+                    };
+                    Services.prefs.setBoolPref(name, value);
+                    prefs.setBoolPref(name, value);
+                    if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) === true) {
+                        Services.prefs.unlockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                    };
+                    if (Services.prefs.getBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                        Services.prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                        prefs.setBoolPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`, true);
+                    };
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}`);
+                };
+                if (Services.prefs.prefIsLocked(name) !== true) {
+                    Services.prefs.lockPref(name);
+                };
+                if (Services.prefs.prefIsLocked(`${ifPrefix}${name}${ifSuffix}${ifModified}`) !== true) {
+                    Services.prefs.lockPref(`${ifPrefix}${name}${ifSuffix}${ifModified}`);
+                };
                 break;
               default:
                 throw new Error(
