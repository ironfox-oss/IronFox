
# Build options that vary depending on the user's environment/prebuild.sh
## (And are only relevant to branded builds...)

readonly MOZ_SOURCE_CHANGESET="${IRONFOX_REVISION}"
export MOZ_SOURCE_CHANGESET
